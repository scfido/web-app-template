import { ComponentPreview } from "@/docs"
import { useTitle } from "ahooks"

import { Button } from "@/components/ui/button";
import { Camera } from "lucide-react";

export const meta = () => [
    { title: "Button" }
]

export const handle = {
    documentType: "mdx"
}

<h1 className="">按钮</h1>

export const Demo1 = () => {
    return (<ComponentPreview title="样式">
        <div className="flex gap-4">
            <Button>默认</Button>
            <Button variant={"secondary"} >次要</Button>
            <Button variant={"outline"}>边框</Button>
            <Button variant={"link"}>链接</Button>
            <Button variant={"ghost"}>幽灵</Button>
            <Button variant={"destructive"}>危险</Button>
        </div>
    </ComponentPreview >
    )
}

export const Demo2 = () => {
    return (<ComponentPreview title="状态" discription="各样式的禁用状态">
        <div className="flex gap-4">
            <Button disabled>默认</Button>
            <Button disabled variant={"secondary"} >次要</Button>
            <Button disabled variant={"outline"}>边框</Button>
            <Button disabled variant={"link"}>链接</Button>
            <Button disabled variant={"ghost"}>幽灵</Button>
            <Button disabled variant={"destructive"}>危险</Button>
        </div>
    </ComponentPreview>
    )
}

export const Demo3 = () => {
    return (<ComponentPreview title="尺寸">
        <div className="flex gap-4">
            <Button size={"lg"} >大</Button>
            <Button >默认</Button>
            <Button size={"sm"} >小</Button>
            <Button size={"icon"} ><Camera /></Button>
        </div>
    </ComponentPreview>)
}

<Demo1/>
<Demo2/>
<Demo3/>

## 使用按钮样式

在其它元素上使用按钮定义的样式。

```tsx
import { buttonVariants } from "@/components/ui/button"

<div 
    className={buttonVariants({ variant: "outline" , size: "lg" })}
>
    测试
</div>
```